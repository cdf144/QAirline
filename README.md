# QAirline

## Set-up

- Install dependencies
  ```sh
  $ npm i
  ```
- Set environment variables:
  - In `apps/api`, copy `.env.example` to `.env`, and configure the `JWT_SECRET` and `MONGODB_URI` variables

## Run

```
$ npm run start
```
